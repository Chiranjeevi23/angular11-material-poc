import { DrawerFormDirective } from './drawer-form.directive';

describe('DrawerFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerFormDirective();
    expect(directive).toBeTruthy();
  });
});
